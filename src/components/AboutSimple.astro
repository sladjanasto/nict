---
import { getUITranslations, getLangFromUrl } from '../utils/i18n';

const { url } = Astro;
const lang = getLangFromUrl(url);
const ui = await getUITranslations(lang);

// Extract about data safely
const aboutData = ui.about || {};
const advantages = aboutData.advantages || [];
const coreCompetencies = aboutData.coreCompetencies?.items || [];
const certifications = aboutData.certifications?.items || [];

// Extract technology expertise data
const portfolioData = ui.portfolio || {};
const technologyExpertise = portfolioData.technologyExpertise || aboutData.technologyExpertise || {};
const techCategories = technologyExpertise.categories || [];
---

<section id="about" class="about section">
	<div class="container">
		<h2 class="section-title scroll-reveal">What makes us tick</h2>
		
		<div class="about-intro scroll-reveal">
			<div class="experience-highlight">
				<h3>20+ Years Hands-On Experience. Strategic Vision. Operational Excellence.</h3>
			</div>
			<p class="intro-text">
				As an IT expert with over two decades of hands-on experience, I bring a unique combination of strategic foresight and operational depth. My career path led me from system administrator to senior global IT Manager role and finally to an independent IT service provider – a journey that enabled me to penetrate and understand every level of the IT landscape.
			</p>
		</div>
		
		<div class="about-content fade-in-up">
			<!-- Core Competencies Section -->
			<div class="competencies-section">
				<h3 class="section-subtitle">{aboutData.coreCompetencies?.title || 'Core Competencies'}</h3>
				<div class="competencies-list">
					{coreCompetencies.map((competency: any) => (
						<div class="competency-row">
							<div class="competency-details">
								<h4>{competency.title}</h4>
								<div class="skills-icons">
									{competency.skills?.map((skill: string) => {
										let iconClass = '';
										let skillName = skill;
										
										// Mapiranje skill-ova na Icomoon ikonice
										switch(skill.toLowerCase()) {
											case 'aws':
												iconClass = 'icon-amazonaws';
												break;
											case 'azure':
												iconClass = 'icon-microsoftazure';
												break;
											case 'google cloud':
												iconClass = 'icon-googlecloud';
												break;
											case 'multi-cloud strategy':
												iconClass = 'icon-cloud';
												skillName = 'Multi-cloud';
												break;
											case 'docker':
												iconClass = 'icon-docker';
												break;
											case 'kubernetes':
												iconClass = 'icon-kubernetes';
												break;
											case 'terraform':
												iconClass = 'icon-terraform';
												break;
											case 'ci/cd':
												iconClass = 'icon-jenkins';
												skillName = 'CI/CD';
												break;
											case 'team leadership':
												iconClass = 'icon-atlassian';
												break;
											case 'project management':
												iconClass = 'icon-atlassian';
												break;
											case 'itil':
												iconClass = 'icon-microsoft';
												break;
											case 'agile':
												iconClass = 'icon-atlassian';
												break;
											case 'scrum master':
												iconClass = 'icon-atlassian';
												break;
											case 'itil v4':
												iconClass = 'icon-microsoft';
												break;
											case 'ai manager':
												iconClass = 'icon-microsoft';
												break;
											default:
												iconClass = 'icon-cogs';
										}
										
										return (
											<div class="skill-icon-item">
												<span class={iconClass}></span>
											</div>
										);
									})}
								</div>
							</div>
						</div>
					))}
				</div>
			</div>
			
			<!-- Key Advantages Section -->
			<div class="advantages-section">
				<h3 class="section-subtitle">Key Advantages</h3>
				<div class="advantages-list">
					{advantages.map((advantage: any) => (
						<div class="advantage-row">
							<div class="advantage-icon">
								<span>{advantage.icon}</span>
							</div>
							<div class="advantage-details">
								<h4>{advantage.title}</h4>
								<p>{advantage.description}</p>
							</div>
						</div>
					))}
				</div>
			</div>
			
		</div>
	</div>
</section>

<style>
	/* Import Icomoon font */
	@import url('/icons/style.css');

	.about {
		background: var(--bg-white);
		padding: var(--spacing-xxl) 0;
	}
	
	.about-intro {
		text-align: center;
		margin-bottom: var(--spacing-xxl);
	}
	
	.intro-text {
		font-size: var(--font-size-lg);
		color: var(--text-muted);
		max-width: 800px;
		margin: 0 auto;
		line-height: 1.7;
	}
	
	.experience-highlight {
		margin-bottom: var(--spacing-lg);
	}
	
	.experience-highlight h3 {
		font-family: var(--font-headings);
		font-size: var(--font-size-xl);
		font-weight: 600;
		color: var(--primary-color);
		text-align: center;
		margin: 0;
		line-height: 1.4;
	}
	
	.about-content {
		max-width: 900px;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		gap: var(--spacing-3xl);
	}
	
	/* Section Subtitles */
	.section-subtitle {
		font-family: var(--font-headings);
		font-size: var(--font-size-2xl);
		font-weight: 700;
		color: var(--primary-color);
		margin-bottom: var(--spacing-xl);
		text-align: center;
		position: relative;
	}
	
	.section-subtitle::after {
		content: '';
		position: absolute;
		bottom: -8px;
		left: 50%;
		transform: translateX(-50%);
		width: 60px;
		height: 3px;
		background: var(--bg-gradient);
		border-radius: 2px;
	}
	
	/* Core Competencies Linear Layout */
	.competencies-list {
		display: flex;
		flex-direction: row;
		gap: var(--spacing-lg);
	}
	
	.competency-row {
		display: flex;
		align-items: flex-start;
		gap: var(--spacing-lg);
		padding: 1rem;
		background: var(--bg-secondary);
		border-radius: 50%;
		border: 1px solid var(--primary-color);
		transition: all var(--transition-normal);
	}
	
	.competency-row:hover {
		transform: translateX(8px);
		box-shadow: var(--shadow-lg);
		background: var(--bg-white);
	}
	
	
	.competency-details {
		flex: 1;
	}
	
	.competency-details h4 {
		font-family: var(--font-headings);
		font-size: var(--font-size-xl);
		font-weight: 600;
		color: var(--text-dark);
		margin-bottom: var(--spacing-md);
	}
	
	.skills-icons {
		display: flex;
		flex-wrap: wrap;
		gap: var(--spacing-lg);
		justify-content: flex-start;
	}
	
	.skill-icon-item {
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: var(--radius-lg);
		transition: all var(--transition-normal);
		width: 60px;
		height: 60px;
		cursor: pointer;
	}
	
	.skill-icon-item:hover {
		color: white;
		transform: translateY(-4px) scale(1.1);
		box-shadow: var(--shadow-lg);
	}
	
	.skill-icon-item span[class^="icon-"] {
		font-size: 2rem;
		line-height: 1;
		transition: all var(--transition-normal);
		color: var(--primary-color);
	}
	
	
	/* Key Advantages Linear Layout */
	.advantages-list {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-lg);
	}
	
	.advantage-row {
		display: flex;
		align-items: flex-start;
		gap: var(--spacing-lg);
		padding: var(--spacing-xl);
		background: var(--bg-secondary);
		border-radius: var(--radius-lg);
		border-left: 4px solid var(--secondary-color);
		transition: all var(--transition-normal);
	}
	
	.advantage-row:hover {
		transform: translateX(8px);
		box-shadow: var(--shadow-lg);
		background: var(--bg-white);
	}
	
	.advantage-icon {
		font-size: 3rem;
		min-width: 60px;
		display: flex;
		align-items: center;
		justify-content: center;
	}
	
	.advantage-details {
		flex: 1;
	}
	
	.advantage-details h4 {
		font-family: var(--font-headings);
		font-size: var(--font-size-xl);
		font-weight: 600;
		color: var(--text-dark);
		margin-bottom: var(--spacing-sm);
	}
	
	.advantage-details p {
		color: var(--text-muted);
		line-height: 1.6;
		font-size: var(--font-size-md);
	}
	
	/* Projects Section Styles */
	.projects-section {
		width: 100%;
	}
	
	.projects-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
		gap: var(--spacing-xl);
	}
	
	.project-card {
		padding: var(--spacing-xl);
		border-radius: var(--radius-lg);
		background: var(--bg-card);
		box-shadow: var(--shadow-md);
		transition: transform var(--transition-normal), box-shadow var(--transition-normal);
		border: 1px solid rgba(26, 234, 226, 0.1);
	}
	
	.project-card:hover {
		transform: translateY(-5px);
		box-shadow: var(--shadow-lg);
		border-color: var(--primary-color);
	}
	
	.project-header {
		display: flex;
		align-items: center;
		gap: var(--spacing-md);
		margin-bottom: var(--spacing-md);
	}
	
	.project-icon {
		font-size: 2rem;
	}
	
	.project-title {
		font-size: var(--font-size-lg);
		font-weight: 600;
		color: var(--text-dark);
		margin: 0;
	}
	
	.project-industry {
		font-size: var(--font-size-sm);
		color: var(--text-light);
	}
	
	.project-description {
		color: var(--text-light);
		margin-bottom: var(--spacing-md);
		line-height: 1.6;
	}
	
	.project-results {
		list-style: none;
		padding: 0;
		margin: 0;
	}
	
	.result-item {
		display: flex;
		align-items: center;
		gap: var(--spacing-xs);
		margin-bottom: var(--spacing-sm);
		font-size: var(--font-size-sm);
		color: var(--text-dark);
	}
	
	.result-icon {
		font-size: var(--font-size-xs);
	}
	
	/* Tablet Styles */
	@media (min-width: 769px) and (max-width: 1024px) {
		.about-content {
			max-width: 800px;
		}
		
		.competency-row,
		.advantage-row {
			flex-direction: column;
			text-align: center;
			gap: var(--spacing-md);
		}
		
		.advantage-icon {
			align-self: center;
		}
	}
	
	@media (max-width: 768px) {
		.experience-highlight h3 {
			font-size: var(--font-size-lg);
		}
		
		.about-content {
			gap: var(--spacing-2xl);
		}
		
		.section-subtitle {
			font-size: var(--font-size-xl);
		}
		
		.competency-row,
		.advantage-row {
			flex-direction: column;
			text-align: center;
			gap: var(--spacing-md);
			padding: var(--spacing-lg);
		}
		
		.advantage-icon {
			align-self: center;
		}
		
		.competency-details h4,
		.advantage-details h4 {
			font-size: var(--font-size-lg);
		}
		
		.skills-icons {
			justify-content: center;
			gap: var(--spacing-md);
		}
		
		.skill-icon-item {
			width: 50px;
			height: 50px;
			padding: var(--spacing-sm);
		}
		
		.skill-icon-item span[class^="icon-"] {
			font-size: 1.5rem;
		}
	}
</style>
</style>